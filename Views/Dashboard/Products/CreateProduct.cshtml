@model OnlineStoreMVC.Models.ViewModels.ProductViewModel
@{
  Layout = "~/Views/Shared/_DashboardLayout.cshtml";
  ViewData["Title"] = "Tạo sản phẩm mới";
}

<div class="d-flex justify-content-center mt-5">
  <div class="card shadow" style="width: 800px;">
    <div class="card-header bg-success text-white">
      <h4 class="mb-0">🏷️ Tạo mới loại</h4>
    </div>

    <div class="card-body">
      @Html.ValidationSummary(true, "", new { @class = "error-message" })

      <form asp-action="Create" method="post" enctype="multipart/form-data">
        <div class="mb-3">
          <label asp-for="ProductName">Tên</label>
          <input asp-for="ProductName" class="form-control" />
          <span asp-validation-for="ProductName" class="text-danger"></span>
        </div>

        <div class="mb-3">
          <label asp-for="Price">Giá</label>
          <input asp-for="Price" class="form-control" type="number" min="1" step="1"
            oninput="this.value = this.value.replace(/[^0-9]/g, '')" />
          <span asp-validation-for="Price" class="text-danger"></span>
        </div>

        <div class="mb-3">
          <label asp-for="CategoryID">Loại</label>
          <select asp-for="CategoryID" class="form-select" asp-items="ViewBag.Categories">
            <option value="">-- Chọn danh mục --</option>
          </select>
          <span asp-validation-for="CategoryID" class="text-danger"></span>
        </div>


        <div class="mb-3">
          <label asp-for="Status">Trạng thái</label>
          <select asp-for="Status" class="form-select">
            <option value="Available">Có sẵn</option>
            <option value="OutOfStock">Hết hàng</option>
            <option value="Promotion">Khuyến mãi</option>
          </select>
          <span asp-validation-for="Status" class="text-danger"></span>
        </div>

        <div class="mb-3">
          <label>Ảnh sản phẩm (có thể chọn nhiều)</label>
          <input type="file" name="Images" multiple class="form-control" />
        </div>

        <div class="mb-3">
          <label asp-for="IsFeatured" class="form-label">Sản Phẩm Nổi bật</label>
          <input asp-for="IsFeatured" type="checkbox" class="form-check-input ms-2" />
          <span asp-validation-for="IsFeatured" class="text-danger"></span>
        </div>


        <div class="mb-3">
          <label asp-for="Description">Ghi chú</label>
          <textarea asp-for="Description" class="form-control"></textarea>
        </div>

        <hr />
        <div class="d-flex justify-content-between align-items-center mb-3">
          <h5 class="text-primary mb-0">🧬 Size, màu, tồn kho sản phẩm</h5>
          <button type="button" class="btn btn-outline-primary" onclick="addVariant()">➕ Thêm</button>
        </div>
        <div id="variant-container">
          @for (int i = 0; i < Model.Variants.Count; i++)
          {
            <div class="variant-item d-flex gap-3 align-items-start mb-3" style="height: 100%;">
              <div class="flex-fill">
                <label>Size</label>
                <input asp-for="Variants[@i].Size" class="form-control" />
                <span asp-validation-for="Variants[@i].Size" class="text-danger"></span>
              </div>
              <div class="flex-fill">
                <label>Màu sắc</label>
                <input asp-for="Variants[@i].Color" class="form-control" />
                <span asp-validation-for="Variants[@i].Color" class="text-danger"></span>
              </div>
              <div class="flex-fill">
                <label>Tồn kho</label>
                <input asp-for="Variants[@i].Stock" class="form-control" type="number" min="1"
                  oninput="this.value = this.value.replace(/[^0-9]/g, '')" />
                <span asp-validation-for="Variants[@i].Stock" class="text-danger"></span>
              </div>
              <div style="height: 100%; padding-top: 15px;">
                <button type="button" class="btn btn-sm btn-danger mt-2" onclick="removeVariant(this)">➖</button>
              </div>
            </div>
          }
        </div>

        <button type="submit" class="btn btn-success">Tạo mới</button>
        <a href="/Dashboard/Products" class="btn btn-secondary ms-2">Quay lại</a>
      </form>
    </div>
  </div>
</div>

@section Scripts {
  <partial name="_ValidationScriptsPartial" />
  <script>
    let variantIndex = @Model.Variants.Count;

    function addVariant() {
      const container = document.getElementById("variant-container");
      const html = `
                                                  <div class="variant-item d-flex gap-3 align-items-start mb-3">
                                                    <div class="flex-fill">
                                                      <label>Size</label>
                                                      <input name="Variants[${variantIndex}].Size" class="form-control" maxlength="10" />
                                                      <span asp-validation-for="Variants[${variantIndex}].Size" class="text-danger"></span>
                                                    </div>
                                                    <div class="flex-fill">
                                                      <label>Màu sắc</label>
                                                      <input name="Variants[${variantIndex}].Color" class="form-control" maxlength="50" />
                                                      <span asp-validation-for="Variants[${variantIndex}].Color" class="text-danger"></span>
                                                    </div>
                                                    <div class="flex-fill">
                                                      <label>Tồn kho</label>
                                                      <input name="Variants[${variantIndex}].Stock" class="form-control" type="number" min="1" 
                                                      oninput="this.value = this.value.replace(/[^0-9]/g, '')"
                                                      />
                                                      <span asp-validation-for="Variants[${variantIndex}].Stock" class="text-danger"></span>
                                                    </div>
                                                    <div style="height: 100%; padding-top: 15px;">
                                                      <button type="button" class="btn btn-sm btn-danger mt-2" onclick="removeVariant(this)">➖</button>
                                                    </div>
                                                  </div>`;
      container.insertAdjacentHTML("beforeend", html);
      variantIndex++;
    }

    function removeVariant(button) {
      const row = button.closest(".variant-item");
      row.remove();
    }
  </script>
}